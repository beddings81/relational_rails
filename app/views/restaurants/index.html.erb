<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h3>- <%= restaurant.name %>- Created_at: <%= restaurant.created_at %> <%= link_to "Update #{restaurant.name}", "/restaurants/#{restaurant.id}/edit" %></h3>
  <% end %>
  
  <%= link_to "New Restaurant", "/restaurants/new" %>