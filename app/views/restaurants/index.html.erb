<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h3>- <%= restaurant.name %>- Created_at: <%= restaurant.created_at %> <%= link_to "Update #{restaurant.name}", "/restaurants/#{restaurant.id}/edit" %> | <%= link_to "Delete #{restaurant.name}", "/restaurants/#{restaurant.id}/destroy" %></h3>
  <% end %>
  
  <%= link_to "New Restaurant", "/restaurants/new" %>